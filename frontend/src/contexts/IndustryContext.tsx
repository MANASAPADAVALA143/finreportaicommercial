/** Re-export IndustryConfig context under the IndustryContext path from the product brief. */
export {
  IndustryConfigProvider,
  IndustryConfigProvider as IndustryProvider,
  useIndustryConfig,
} from '../context/IndustryConfigContext';
